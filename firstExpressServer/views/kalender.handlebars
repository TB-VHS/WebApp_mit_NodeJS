<ul id="dropdown-left" class="dropdown-content">
  <li><a href="/logout">logout</a></li>
</ul>
<nav>
  <div class="nav-wrapper light-blue darken-3">
    <a class="dropdown-trigger" href="#!" data-target="dropdown-left"><i class="large material-icons left">more_vert</i></a>
    <div class="brand-logo center"><h4>{{ displayName }} - {{ monatJetzt }}</h4></div>
    <ul class="right">
      <li><a href="#"><i class="material-icons">chevron_left</i></a></li>
      <li><a href="#"><i class="material-icons">chevron_right</i></a></li>
    </ul>
  </div>
</nav>

<div id="main">
      <table class="striped">
        <tbody>
          {{#each kalenderTage as |tag i| }}
            {{#if this.calTag}}
              <tr>
                <td class="tageszahl">
                  {{ this.calTag.tageszahl }}
                </td>
                <td class="wochentag">
                  {{ this.calTag.wochentag }}
                </td>
                <td class="termin">
                  <div id="{{ this.epoche }}">
                  </div>
                </td>

              </tr>
            {{/if}}
          {{/each}}
        </tbody>
      </table>
</div>
<script charset="utf-8">
  $(".dropdown-trigger").dropdown();
</script>
